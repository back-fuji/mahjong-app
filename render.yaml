services:
  - type: web
    name: mahjong-server
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npx tsx server/index.ts
    envVars:
      - key: PORT
        value: 3001
      - key: CLIENT_URL
        sync: false
